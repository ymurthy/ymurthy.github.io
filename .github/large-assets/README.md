# Large PDF assets

The thesis PDFs are stored here as numbered binary parts. The GitHub Pages workflow reassembles them and verifies their SHA-256 checksums before building the website, so the published downloads are byte-for-byte identical to the originals.
